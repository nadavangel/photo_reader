# Build and packaging dependencies for photo_reader
# These are only needed when building standalone executables with PyInstaller
# 
# Usage:
#   pip install -r requirements-build.txt
#
# Then build the executable:
#   pyinstaller splitToWells.spec

altgraph==0.17.3
pefile==2023.2.7
pyinstaller==5.13.2
pyinstaller-hooks-contrib==2023.8
pywin32-ctypes==0.2.2
